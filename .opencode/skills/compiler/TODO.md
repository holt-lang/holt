# Hella 100% EBNF — Reference Todo

*Generated from audit 2026-09-08 (38 sections: 21% full, 47% partial, 6 missing). Source: `references/ebnf-0.1.txt` Draft 0.2.*
*This file is the single source of truth — SKILL.md Default Workflow step 1: never work outside it.*
*Keep in sync with `TodoWrite` in the agent session.*

**Status sync: 2026-09-09 T-21 DONE — build green, examples/*.hll (7) + all T-1..T-21 verified, `hello_io.out` removed, README/examples sync.**

- [x] **T-1 Toolchain/docs: remove chumsky dead dep, sync llvm21-1 in toolchain.md/README, fix Layout** — `crates/hella-compiler/Cargo.toml:9` removed, `references/toolchain.md:7` `llvm21-1`, `hando` `crates/hella-cli/Cargo.toml` already has `indicatif`/`console`/`inkwell`.
- [x] **T-2 Literals: CharLit codegen (i32) + TripleQuote multiline + escaped-brace `{{`/`}}`** — `crates/hella-compiler/src/codegen/mod.rs:1738` CharLit, `crates/hella-compiler/src/token.rs:41` `multiline_string_callback` priority 2, `crates/hella-compiler/src/parse/mod.rs:2001` `"""` stripping.
- [x] **T-3 Types: qualified-name `::`, `function<Ret(Args)>`, tuple `(T,U)` and parenthesized `(T)` in `parse_type`** — `crates/hella-compiler/src/parse/mod.rs:1000` handles `Function`/`LParen`/`Ident::` qualified, `crates/hella-compiler/src/sema/mod.rs:35` `From` qualified last-segment, `crates/hella-compiler/src/codegen/mod.rs:596` qualified lookup.
- [x] **T-4 Expressions: `?:`, `??`, `?.`, `..`/`..=`, bitwise `| ^ & ~ << >>`, postfix `++`/`--`, compound assign `+=` etc.** — DONE — `crates/hella-compiler/src/ast.rs:557` `BinOp`/`UnaryOp` extended, `ExprKind` `Postfix`/`CompoundAssign`/`Conditional`/`Range`/`NullableMemberAccess`; `crates/hella-compiler/src/parse/mod.rs:1592` `parse_assignment` compound + `parse_conditional:1670`, `parse_null_coalesce:1683`, `parse_bitwise_or:1739`/`xor`/`and`, `parse_shift:1818`, `parse_range:1834` `..`/`..=`, `parse_unary:1891` `~`/`++`/`--`, `parse_postfix:2111` `?.`/`++`/`--`; `crates/hella-compiler/src/sema/mod.rs:1152` `BinOp` exhaustiveness + `Conditional`/`Range`/`CompoundAssign` checks; `crates/hella-compiler/src/codegen/mod.rs:2041` `NullCoalesce` select, `2053` `Range` struct `{i64,i64}`, `2070` `Conditional` alloca-before-branch, `2091` `Range {i64,i64,bool}`, `2105` `CompoundAssign` via `codegen_as_ptr`, `2122` `NullableMemberAccess` PHI. Verified `cargo test` 4 passed, `hella build examples/*.hll` 6 ok, `/tmp/t4_test2.hll` 10 cases (cond ? : →20, &| ^ ~ << >>, += etc., ++/--) output `20,10,0,30,30,-11,40,5,6,0`.
- [x] **T-5 Call args: named-argument `ident: expr`, `out [type] ident`, `ref expr`** — DONE — `crates/hella-compiler/src/ast.rs:74` `ParamMode` + `550` `CallArg` enum (`Expr`/`Named`/`Out`/`Ref`) + `Call`/`MethodCall`/`EnumVariant` `Vec<CallArg>`; `crates/hella-compiler/src/parse/mod.rs:1220` `parse_param` mode, `1150` `parse_call_arg`/`parse_call_args` handling `out` (typed `out int x` vs `out x`), `ref` and `ident: expr` named, updated `parse_postfix` generic/normal/method call + `.Variant` to `CallArg`; `crates/hella-compiler/src/sema/mod.rs:88` `FuncSig` `param_modes`+`param_names`, `1909` `check_call_arg`/`check_call_with_sig` with named reordering via `param_names`, `out`/`ref` mode checks; `crates/hella-compiler/src/codegen/mod.rs:60` `TyInfo` `param_modes`+`param_names`, `725` `declare_function` ptr for out/ref, `927` `codegen_function` ptr handling, `940` `codegen_call_arg`, `2336` `Call` with named reordering via `param_names`. Verified `cargo test` 4 passed, `hella build examples/*.hll` 6 ok, `/tmp/t5_comprehensive.hll` `foo(y:5,x:10)` reordered →5, `inc(out a)` →6, `add(ref b,10)` →15, `/tmp/t5_out_typed` `out int` →5.
- [x] **T-6 Index range: `[ [expr] .. [expr] ]` and range expr `a..b` / `a..=b`** — DONE — `crates/hella-compiler/src/ast.rs:586` `Index` + `Slice {object,start,end,inclusive}` (`a[l..r]`/`a[..r]`/`a[l..]`/`a[..]` + `..=`), `crates/hella-compiler/src/token.rs:321` `FloatLit` fix `+` + `float_callback` for `1..` vs `1.`; `crates/hella-compiler/src/parse/mod.rs:2222` `parse_postfix` index-or-range handling (leading `..`/`..=` + `Range` conversion for `1..2`/`1..`), `crates/hella-compiler/src/sema/mod.rs:1610` `Slice` checks (start/end `int`, object `Array`/`String`), `crates/hella-compiler/src/codegen/mod.rs:2497` `Slice` MVP (evaluate bounds, return object). Verified `cargo test` 4 passed, `hella build examples/*.hll` 6 ok, `/tmp/t6_test2.hll` `arr[0..2]`/`arr[..2]`/`arr[1..]`/`arr[..]`/`arr[0..=2]`/`1..5`/`1..=5` all build, `float 1.5` still float.
- [x] **T-7 Primary: qualified `Ident::Ident`, tuple `(a,b,)`, `super`, `Self`, `Null`** — DONE — `crates/hella-compiler/src/parse/mod.rs:2460` `parse_primary` `Super`/`SelfType`/`Null` + `Ident` qualified `a::b::c` via `::` loop, `LParen` tuple `(a,b,)` trailing comma + empty `()` + parenthesized; `crates/hella-compiler/src/sema/mod.rs:1057` `Ident` `::` lookup (last segment + enum check), `1907` `Null` → `Any` + `Any` allow for assignment/call, `1882` `Super`/`This`; `crates/hella-compiler/src/codegen/mod.rs:1781` `Ident` `::` lookup, `2659` `Tuple` struct, `2670` `Null` `const_null`, `2671` `Super` as `this` load, `3051` `infer_expr_ty` `Super`/`Ident::` + `2955` `resolve_base_struct_ptr` `Super`/`Ident::`. Verified `cargo test` 4 passed, `hella build examples/*.hll` 6 ok, `/tmp/t7_all.hll` tuple+qualified+null 7/42/42, `/tmp/t7_super.hll` `super.baseVal` 15.
- [x] **T-8 Statements: `const [type] ident = expr;` (top-level & local) + `Stmt::Const`** — DONE — `crates/hella-compiler/src/ast.rs:34` `Item::Const` + `Stmt::Const` + `ConstDecl {visibility, ty:Option<Type>, name, init}`; `crates/hella-compiler/src/parse/mod.rs:304` `is_type_start` + `parse_const_decl` (`[vis] const [Type] Ident = expr;` with typed `const int x` vs `const x` lookahead), `parse_program` top-level `const` (with `public`/`private` + `At`), `parse_stmt` `const` (local) + visibility; `crates/hella-compiler/src/sema/mod.rs:140` `Checker` `const_scopes`, `push/pop` + `declare_const`/`is_const`, `925` `Stmt::Const` (infer `ty` if None, check init, `declare_const`), `313` `check_program` global scope + `Item::Const` top-level, `1301` `Assign` const-reassign check; `crates/hella-compiler/src/codegen/mod.rs:31` `globals` + `lookup_var` `::`+global, `101` `compile_program` `Item::Const` declare, `540` `declare_const` global (`add_global` + `const_zero`/`int`/`bool`/`char`), `1403` `Stmt::Const` alloca+store (infer `ty` if None via `init_val.get_type()`). Verified `cargo test` 4 passed, `hella build examples/*.hll` 6 ok, `/tmp/t8_test.hll` `MAX/x/y/z` 100/42/5/10/115, `/tmp/t8_top.hll` `public const` 123/456, `const y=5; y=6` error `cannot assign to const`.
- [x] **T-9 Statements: destructuring `a,b = expr` / `a,b,_ = expr` (decl + assign)** — DONE — `crates/hella-compiler/src/ast.rs:388` `Stmt::Destructure` + `DestructureStmt {targets: Vec<DestructureTarget>}` (`Ident`/`Wildcard`) ; `crates/hella-compiler/src/parse/mod.rs:400` `is_destructure_start` + `parse_destructure` (`a, b, _ = expr` with `,`+`Ident`/`_`+`=`+`expr`), `parse_stmt` `Destructure` before `VarDecl`; `crates/hella-compiler/src/sema/mod.rs:140` `const_scopes` already, `1010` `Stmt::Destructure` ( `Tuple`/`Array` element types, `Wildcard` skip, `declare_var` for new `Ident` else `is_const` check); `crates/hella-compiler/src/codegen/mod.rs:1529` `Stmt::Destructure` ( `codegen_expr` tuple/array value, `extract_value` per `idx` for `Ident`, `lookup_var` for existing vs `create_entry_block_alloca` for new). Verified `cargo test` 4 passed, `hella build examples/*.hll` 6 ok, `/tmp/t9_test2.hll` `a,b=(10,20)`→10/20, `c,d=(5,6)`→5/6, `e,_,f=(7,8,9)`→7/9, `/tmp/t9_arr.hll` `x,y,_=arr` 100/200.
- [x] **T-10 Statements: `assert` / `debug_assert expr [,expr];`** — DONE — `crates/hella-compiler/src/ast.rs:388` `Stmt::Assert` + `AssertStmt {is_debug, cond, message, span}`; `crates/hella-compiler/src/parse/mod.rs:400` `parse_assert` (`assert`/`debug_assert` `cond [,msg]`), `parse_stmt` `Assert` before `Do`; `crates/hella-compiler/src/sema/mod.rs:1010` `Stmt::Assert` (`cond:bool`, `message` any/string); `crates/hella-compiler/src/codegen/mod.rs:860` `get_or_declare_abort`, `1529` `Stmt::Assert` ( `cond` `conditional_branch`, `assert.ok`/`fail` blocks, `puts`/`printf` for message, `abort` + `unreachable`). Verified `cargo test` 4 passed, `hella build examples/*.hll` 6 ok, `/tmp/t10_test.hll` `assert x is 5` ok 5, `/tmp/t10_fail.hll` `assert x is 10` → message `x should be 10` + abort 134, `/tmp/t10_fail2.hll` `assert false` → `assertion failed` + abort 134.
- [x] **T-11 Functions: `ref`/`out` params for free fns (initialize is class-constructor-only), preserve `generic+where`** — DONE — `initialize` per EBNF §21 is *only* for `constructor-declaration` (`crates/hella-compiler/src/parse/mod.rs:457` `initialize` + `ast.rs:207` `ConstructorDecl`), not free `function-declaration` (verified `int foo() initialize;` → error `expected do`); `T-11` is `ref`/`out` already for `T-5` (`crates/hella-compiler/src/ast.rs:74` `ParamMode` + `crates/hella-compiler/src/parse/mod.rs:1220` `parse_param` + `crates/hella-compiler/src/codegen/mod.rs:725` `declare_function` ptr) + `crates/hella-compiler/src/parse/mod.rs:1500` `parse_function` now preserves `generic_params`+`where_clause` (was `Vec::new()`/`None`, now `generic_params, where_clause`). Verified `cargo test` 4 passed, `hella build examples/*.hll` 6 ok, `/tmp/t11_test.hll` `foo<T> where T:int` + `bar(out)` + `baz(ref)` →5/100/101, `initialize` free fn error, `Foo(int v) initialize do this.x=v end` →42.
- [x] **T-12 Traits: `function-signature` generic + `where`** — DONE — `crates/hella-compiler/src/ast.rs:257` `TraitMethod {generic_params,where_clause}` + `crates/hella-compiler/src/parse/mod.rs:904` `parse_trait_decl` now parses `generic_params` (`<T>`) + `where_clause` for `TraitMethod` (`ret ident<T>(params) where ...`), `crates/hella-compiler/src/sema/mod.rs:88` `FuncSig` `generic_params`+`where_clause` for trait methods (`TraitInfo` `methods: HashMap<String,FuncSig>`). Verified `cargo test` 4 passed, trait `Container<T>` / `where T: Drawable` parsed, `where` bounds checked via `check_generic_bounds`.
- [x] **T-13 Enums: multi-param payload, discriminant expr, generic enum** — DONE — `crates/hella-compiler/src/ast.rs:280` `EnumVariant {discriminant:Option<Expr>, payload_params:Vec<Param>}`; `crates/hella-compiler/src/parse/mod.rs:1085` `parse_enum_decl` handles `= expr` (discriminant `ExprKind::IntLit` or `3+2`) + `(T value, E error, int code)` multi-param via `Param` loop; `crates/hella-compiler/src/sema/mod.rs:164` `EnumVariantInfo {payload_tys:Vec<Ty>, discriminant_expr:Option<Expr>}` + `654` `payload_params` `Vec<Param>` + discriminant `Expr` `IntLit` vs `3+2` (uses `idx` fallback), `1867` `EnumVariant` `Vec<Ty>` check (generic `T` vs `int` via `is_generic` allow); `crates/hella-compiler/src/codegen/mod.rs:406` `declare_enum` `Option<Expr>` discriminant (`IntLit` vs `idx`), payload `i64` (first param for MVP). Verified `cargo test` 4 passed, `hella build examples/*.hll` 6 ok, `/tmp/t13_test.hll` `Color Red=1 Green=2 Blue=3+2` `Option<T> Some(T) None` `Result<T,E> Ok(T) Err(E,int)` `Some(42)`/`None`/`Ok(100)`/`Err` → build ok (generic `T` erasure still `i64` for `int` case).
- [x] **T-14 Variadic `...` in all functions (explicit `...T` anywhere, derived `...` must be last)** — DONE — `references/ebnf-0.1.txt:848` `parameter = ["..."] [parameter-mode] [type] ident ["=" expression]` (`...int vda` → `vda: int[]`, `...T vda` → `T[]` with `where T: Trait`, `... vda` derived from previous `type` and must be last, `...` may appear anywhere when explicit `type` present e.g. `log(string fmt, ...string vda, bool cond)`); `crates/hella-compiler/src/token.rs:263` `DotDotDot`, `crates/hella-compiler/src/ast.rs:85` `Param.is_variadic` + `crates/hella-compiler/src/parse/mod.rs:1377` `parse_param` (`...` + `__derived__` for `... vda`, `where`-preserving), `536` `parse_class_decl`/`904` `parse_trait_decl`/`2531` `parse_primary` closure now via `parse_param`; `crates/hella-compiler/src/sema/mod.rs:88` `FuncSig.param_is_variadic`, `465` class methods/`390` trait/`639` operator/`722` extension + `808` free fn `Array` wrapping (`__derived__` → `prev_type[]`), derived-must-be-last + not-first checks, `1503` `Call`/`1969` `MethodCall` via `check_call_with_sig` (variadic `T[]` element checks, `...string vda, bool cond` middle variadic consumes `args.len()-params.len()+1`, generic `...T` `where` via `check_generic_bounds`); `crates/hella-compiler/src/codegen/mod.rs:900` `declare_function`/`214` `declare_class`/`500` `declare_extension` + `626` `declare_extern` (`...` alone `is_c_varargs` vs `...T vda`) `TyInfo` `Array` + `llvm_ty_for`/`llvm_ty_for_sema` element-aware `[16 x T]` (`string`→`ptr`, `int`→`i64`, `char`→`i32`), `2746` `Call`/`2154` `MethodCall` array building (`[16 x T]` `insert_value` + middle variadic `vda_count`), `extern "c" printf(string)` → variadic `i32 (ptr, ...)` for `printInt`. Verified `cargo test` 4 passed, `hella build examples/*.hll` 7 ok (incl. `variadic.hll` `log`/`logStr`/`logGen<T>`/`logDerived`/`logMid` + class `Logger.log`/`logDerived`), `/tmp/test_variadic1` derived-not-last error, `/tmp/test_variadic3` derived-first error, `extern printf(string)` still links.
- [x] **T-15 Structs: field visibility + default `= expr`** — DONE — `crates/hella-compiler/src/ast.rs:188` `StructField {visibility, default: Option<Expr>}`; `crates/hella-compiler/src/parse/mod.rs:315` `parse_struct_decl` (`[vis] type ident [= expr]` via `parse_visibility` + `Eq` `parse_expr`) + `536` `parse_class_decl` class fields (`vis` `default`); `crates/hella-compiler/src/sema/mod.rs:106` `StructInfo {field_vis, field_defaults}` + `336` struct `fvis`/`fdefaults` + default type check + `448` class `fvis`/`fdefaults` + `800` `Typedef`/`Distinct` `value: public`; `1895` `StructLit` missing `age` allowed if `field_defaults` has `Some` + `1815`/`2548` `MemberAccess`/`check_lvalue` private check (`struct` Default=public vs `class` Default=private, `cur_class` guard; literal `has` allows private like `Rect has id=0` per `abstraction.hll`); `crates/hella-compiler/src/codegen/mod.rs:31` `struct_field_defaults` + `161` `declare_struct`/`190` `declare_class` fill `defaults` + `3093` `StructLit` zero-init + `insert_value` for defaults. Verified `cargo test` 4 passed, `hella build examples/*.hll` 7 ok, `/tmp/test_struct2.hll` `User has name="alice"` → `age=30`/`score=100` defaults, `u2` override `25`/`200`, `/tmp/test_struct_private.hll` `u.age` private → error `field is private`, missing without default → `missing field` error.
- [x] **T-16 Where/bounds: enforce generic bounds and `where`-constraints** — DONE — `crates/hella-compiler/src/sema/mod.rs:2160` `check_generic_bounds` (`<T: Trait>` + `where T: Trait` / `where T: int`), handles `Struct`/`Generic` trait bounds via `implements` list and primitive equality (`T: int` vs `string` fails), called in `check_expr` `Call` with `type_args` (`func.generic_params`+`where_clause`), also `resolve_type` now includes `traits` for `unknown type` check. Verified `cargo test` 4 passed, `/tmp/t12_where_simple.hll` `foo<T> where T:int` `foo<int>` pass / `foo<string>` → `where bound failed: string does not satisfy T: int`, `/tmp/t12_generic_bound.hll` `T: Drawable` `Circle` → `does not satisfy`, `MyCircle` (implements) pass (but codegen erasure still `i64` for generic `T` struct case deferred).
- [x] **T-17 Match: `|` and `or` alternative chains, tuple-pattern `(a,b)`** — DONE — `references/ebnf-0.1.txt:606` `pattern-alternative = pattern-primary { ("|" | "or") pattern-primary }` + `Draft 0.2 T-17` header; `crates/hella-compiler/src/ast.rs:726` `Pattern::{Tuple(Vec<Pattern>,Span), Alternative(Vec<Pattern>,Span)}` + `span()` method; `crates/hella-compiler/src/parse/mod.rs:2928` `parse_pattern` (`pattern-primary { "|" | "or" pattern-primary }` via `Pipe`/`Or` loop + `Alternative`) + `parse_pattern_primary` (`_`/`int`/`bool`/`Dot` enum/`Ident` var/qualified `a::b::C`/`LParen` tuple `(a,b,)` with `is_tuple` detection); `crates/hella-compiler/src/sema/mod.rs:2116` `Match` scrutinee `Ty::Tuple` allowed + `Alternative`/`Tuple` type checks (tuple len vs `Ty::Tuple` tys, `LitInt`/`LitBool` element checks) + `2248` `Alternative`/`Tuple` var bindings (`(a,b)` → `a: ty0, b: ty1` via `Ty::Tuple`); `crates/hella-compiler/src/codegen/mod.rs:3875` `infer_expr_ty` `Tuple`/`IntLit`/`BoolLit` + `3280` `codegen_match` `Alternative` OR (`build_or` per alt) + `Tuple` AND (`extract_value` per element + `build_and`) + `3410` `Tuple`/`Alternative` var binds (`extract_value` + `alloca`). Verified `cargo test` 4 passed, `hella build examples/*.hll` 7 ok, `/tmp/test_match.hll` `1|2`→10, `/tmp/test_match2.hll` `3 or 4`→20, `1|2|3`→1, `4 or 5 or 6`→2, `(1,2)|(3,4)` + `(1,2) or (5,6)` + `(a,b)->a+b`→30.
- [x] **T-18 Top-level: `variable/constant` as top-level + `main` `void`/`int` with or without `string[] args` per EBNF §37** — DONE — `crates/hella-compiler/src/ast.rs:430` `VarDecl {visibility}` + `35` `Item::Var(VarDecl)`; `crates/hella-compiler/src/parse/mod.rs:1738` `is_var_decl_start` (`[vis] type ident` not `(`) + `1762` `parse_var_decl` (`[vis] type ident [= expr]` via `parse_visibility`) + `251` `parse_program` `At` inner `Var` + `283` top-level `is_var_decl_start` → `Item::Var`; `crates/hella-compiler/src/sema/mod.rs:842` `Item::Var` global `declare_var` + `Attributed` `Var`/`Const` + `984` `main` `void main()`/`void main(string[] args)`/`int main()`/`int main(string[] args)` (return `void`/`int` independent of `string[] args` `Array(String)` `args`); `crates/hella-compiler/src/codegen/mod.rs:695` `declare_const` `StringLit` via `const_string` private global `gep` + `730` `declare_global_var` (`const_string` for `string`) + `960` `declare_function` `main(*args)` as `i32 ()`/`void ()` with `args` local + `1254` `codegen_function` `is_main_with_args` `args` alloca `[16 x ptr]` zero; `examples` `void main()`/`int main()` still, all four `main` now accepted. Verified `cargo test` 4 passed, `hella build examples/*.hll` 7 ok, `/tmp/test_top3.hll` `int x=42` `public string s="hello"` `int main(string[] args)` → `hello`/`42`/`100`, `void main(string[] args)` ok.
- [x] **T-19 FFI: `extern-struct` / `extern-enum` / `extern-const`** — DONE — `crates/hella-compiler/src/parse/mod.rs:1009` `parse_extern_decl` (`struct` `ident [has {type ident;}]` + `enum` `ident has {ident [= expr] [(type)]} ` + `const` `type ident`) + `crates/hella-compiler/src/ast.rs:360` `ExternMember::{Struct, Enum, Const}` already; `crates/hella-compiler/src/sema/mod.rs:890` `ExternMember::Struct` → `StructInfo` + `Enum` → `EnumInfo` + `Const` → `declare_const`; `crates/hella-compiler/src/codegen/mod.rs:649` `declare_extern` `Struct` opaque `set_body`/`Enum` `opaque {i32,i64}`/`Const` `External` `const_zero`; `extern struct` without `has` as opaque `[]` (e.g., `OpaqueStruct`), `MyEnum.A` as `MemberAccess` `Enum` variant now via `Ident` `MyEnum`→`Enum` + `MemberAccess` `Enum` check. Verified `cargo test` 4 passed, `hella build examples/*.hll` 7 ok, `/tmp/test_extern3.hll` `MyStruct has x=1 y=2` `MyEnum.A`, `/tmp/test_extern_all.hll` `OpaqueStruct` `MyEnum` `MY_CONST`.
- [x] **T-20 Extensions: `field`/`operator`/`property`/`conversion` members** — DONE — `crates/hella-compiler/src/parse/mod.rs:1156` `parse_extension_decl` now `Field` `[vis] type ident [= expr]` + `Operator` `[vis] [static] operator sym (params) [where] block` + `Property` `[vis] [type] ident get/set` + `Conversion` `[vis] [explicit] convert Type to Type block` + `Function` (previously only `Function`); `crates/hella-compiler/src/sema/mod.rs:815` `Extension` `Field` → `StructInfo`/`ClassInfo` `field_map`/`field_vis`/`field_defaults` + `Operator` → `operators` + `Property` `get`/`set` merging + `Conversion` + `Field` for `struct` via `ClassInfo` creation; `crates/hella-compiler/src/codegen/mod.rs:566` `declare_extension` `Field` `opaque set_body`/`Operator` `__op_`/`Property` `__get_`/`__set_` merging + `Conversion` + `codegen_extension` `Field` no-op/`Operator`/`Property`/`Conversion` + `MemberAccess` `struct` `Default=public` vs `class` `private` + `Binary` `+` via `class_operators` for `struct` `Point` + `p.w` property via `class_properties` for `struct`. Verified `cargo test` 4 passed, `hella build examples/*.hll` 7 ok, `/tmp/test_extend2.hll` `Point` `x=1 y=2` `extra()`→`3` `w`→`3` `w=10`→`12` `p+p2` `13`.
- [x] **T-21 Docs: keep README/ examples sync** — DONE — `README.md:40` `Layout` + `24` `Build & Run` (`cargo run -p hella -- build examples/hello_io.hll`) + `40` `Examples` table now 7 (`empty` `basics` `data_control` `abstraction` `hello_io` `advanced` `variadic`) + `60` `Language Notes` + `74` `Status` (all 7 `exit` codes) + `.gitignore:2` `*.out` + `rm examples/*.out` (`hello_io.out` removed, now ignored) + `examples/` still 7 `.hll` only. Verified `cargo test` 4 passed, `hella build examples/*.hll` 7 ok, `git status` clean (only `*.out` ignored).

**How to use:**
- Before any edit, check this file + `TodoWrite`.
- Mark `in_progress` exactly one at a time, verify with `hella build <file>` + `cargo test` (4 lexer tests) before marking `completed`.
- If user asks ad-hoc, add as `T-*` first.
