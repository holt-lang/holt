// hello_io.hlt — stdlib import and IO (Phase 1 + stdlib)
// Demonstrates `import std::io` and the intrinsified print functions.

import std::io

void main() do
    println("Hello, Holt!")
    print("no newline: ")
    println("continued")
    printInt(2026)
    putChar('!')
    println("")
end
