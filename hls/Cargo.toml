[package]
name = "hls"
version = "0.1.0"
edition = "2024"

[dependencies]
compiler = { path = "../compiler" }
lsp-server = "0.7.8"
lsp-types = "0.97"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
name = "hls"
path = "src/lib.rs"
